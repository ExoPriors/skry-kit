services:
  scry:
    build: .
    image: skry-kit-sandbox:latest
    working_dir: /workspace
    environment:
      HOME: /home/dev
      SCRY_API_KEY: ${SCRY_API_KEY}
      SCRY_PUBLIC_KEY: ${SCRY_PUBLIC_KEY}
    volumes:
      - ..:/workspace
      - scry-home:/home/dev
    stdin_open: true
    tty: true

volumes:
  scry-home:
